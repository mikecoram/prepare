Where to put roles array/enum?
Add role to user model
Add details about declaring a foreign key in README.md
Add seeder that creates privileges and admin role
Restrict admin page (add admin user in seed?)
User table on admin page
Allow admin to modify users
User permissions
Pull form buttons to the right without breaking error message formatting

Add more testing
    Client-side validation
    Server-side validation
    Page tests
    Auth tests
    App tests?